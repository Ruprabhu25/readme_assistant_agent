{
  "name": "telcor_ai_assistant",
  "version": "0.1.0",
  "description": "README Assistant CLI agent built with the Vercel AI SDK",
  "type": "module",
  "bin": {
    "readme-assistant": "./dist/cli.js"
  },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsc -p tsconfig.json",
    "build:bin": "node scripts/build-bin.mjs && node --experimental-sea-config sea-config.json",
    "build:exe": "node scripts/make-exe.mjs",
    "start": "node dist/cli.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@ai-sdk/openai": "^4.0.4",
    "ai": "^7.0.9",
    "dotenv": "^16.4.5",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "@vitest/coverage-v8": "^2.1.8",
    "esbuild": "^0.28.1",
    "postject": "^1.0.0-alpha.6",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
